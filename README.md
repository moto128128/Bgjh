# Bgjh

This site is now configured to publish to GitHub Pages automatically.

## Publishing setup
- A GitHub Actions workflow is included at [.github/workflows/deploy-pages.yml](.github/workflows/deploy-pages.yml).
- The workflow runs on every push to the main branch and publishes the site.
- A [.nojekyll](.nojekyll) file is included so static assets are served correctly.

## Next step
Push these changes to GitHub and open the repository's Pages settings to ensure the source is set to GitHub Actions.
